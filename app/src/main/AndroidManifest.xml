<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false" />

    <application
        android:name=".AIResumeApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIResumeBuilder"
        android:hardwareAccelerated="true"
        tools:targetApi="31">

        <!-- Splash / Lock Screen -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AIResumeBuilder.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lock Screen -->
        <activity
            android:name=".activities.LockActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Main Dashboard -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Profile Activities -->
        <activity android:name=".activities.ProfileActivity" android:exported="false" />
        <activity android:name=".activities.EditProfileActivity" android:exported="false" />

        <!-- Resume Activities -->
        <activity android:name=".activities.ResumeListActivity" android:exported="false" />
        <activity android:name=".activities.ResumeBuilderActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.ResumePreviewActivity" android:exported="false" />
        <activity android:name=".activities.TemplatesActivity" android:exported="false" />

        <!-- AI Feature Activities -->
        <activity android:name=".activities.AiReviewActivity" android:exported="false" />
        <activity android:name=".activities.CoverLetterActivity" android:exported="false" />
        <activity android:name=".activities.InterviewPrepActivity" android:exported="false" />
        <activity android:name=".activities.CareerCoachActivity" android:exported="false" />
        <activity android:name=".activities.PortfolioActivity" android:exported="false" />

        <!-- Job Tracker -->
        <activity android:name=".activities.JobTrackerActivity" android:exported="false" />
        <activity android:name=".activities.AddJobActivity" android:exported="false" />

        <!-- Analytics -->
        <activity android:name=".activities.AnalyticsActivity" android:exported="false" />

        <!-- Settings -->
        <activity android:name=".activities.SettingsActivity" android:exported="false" />
        <activity android:name=".activities.AiSettingsActivity" android:exported="false" />
        <activity android:name=".activities.AppearanceActivity" android:exported="false" />
        <activity android:name=".activities.SecurityActivity" android:exported="false" />
        <activity android:name=".activities.BackupActivity" android:exported="false" />
        <activity android:name=".activities.NotificationSettingsActivity" android:exported="false" />
        <activity android:name=".activities.AboutActivity" android:exported="false" />
        <activity android:name=".activities.HelpActivity" android:exported="false" />
        <activity android:name=".activities.DeveloperActivity" android:exported="false" />

        <!-- Search -->
        <activity android:name=".activities.SearchActivity" android:exported="false" />

        <!-- Favorites -->
        <activity android:name=".activities.FavoritesActivity" android:exported="false" />

        <!-- Notification Receiver -->
        <receiver
            android:name=".notifications.NotificationReceiver"
            android:exported="false" />

        <receiver
            android:name=".notifications.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing exported resume files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
